import { Box } from "@chakra-ui/layout";
import { SMSTable } from "../components";

const SmsTemplating = () => {
    return (
        <Box>
            <SMSTable />
        </Box>
    );
}

export default SmsTemplating;